pipeline {
    agent any
stages {  
    stage('checkout') {
        steps {
                    checkout scm
}
    }

        stage('Build & Test') {
            steps {
                sh 'mvn clean install'
            }
        }
    }
}
